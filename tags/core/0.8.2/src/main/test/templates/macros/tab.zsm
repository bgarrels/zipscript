[#macro pane id title=null page]
	<div id="${id}" class="tab">
		[#if title!=null]
			<label>${title}</label>
		[/#if]
		[#foreach page in page]
			[#call cmn.page with page/]
		[/#foreach]
	</div>
[/#macro]