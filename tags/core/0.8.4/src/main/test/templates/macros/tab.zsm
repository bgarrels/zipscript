[#macro pane *id title *page[*title selected]]
	<div id="${id}" class="tab">
		[#if title!=null]
			<label>${title}</label>
		[/#if]
		[#foreach page in page]
			[#call cmn.page with page/]
		[/#foreach]
	</div>
[/#macro]